import Navbar from '../components/Navbar';
import Footer from '../components/Footer';
import GigCard from '../components/GigCard';
import Filters from '../components/Filters';

export default function Home() {
  return (
    <div>
      <Navbar />
      <section className="relative bg-gradient-to-r from-purple-600 to-indigo-700 text-white">
        <div className="max-w-7xl mx-auto px-6 py-16 text-center">
          <h1 className="text-4- font-bold">Find the Best Freelancers for Your Projects</h1>
          <p className="mt-4 text-lg">Hire trusted talent globally with secure crypto escrow.</p>
          <div className="mt-6 flex justify-center">
            <input 
              type="text" 
              placeholder="Search gigs, jobs, or services..." 
              className="p-3 rounded-l-md w-1/2 text-black"
            />
            <button className="bg-yellow-500 px-6 py-3 rounded-r-md">Search</button>
          </div>
        </div>
      </section>

      <Filters />
      
      <section className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 p-8">
        <GigCard title="Logo Design" price="$50" seller="Jane D." badge="VynixChoice" />
        <GigCard title="Web Development" price="$300" seller="John S." badge="RisenTalent" />
      </section>

      <Footer />
    </div>
  );
}
